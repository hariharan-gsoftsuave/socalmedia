import React from "react";

const HaterInfo = ( {text} ) => {

    return (
        <div className="hater-info px-4 rounded w-100 text-center">
            <p>{text}</p>
        </div>
    )
};

export default HaterInfo;